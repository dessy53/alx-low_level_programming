#include <stdio.h>
#include "main.h"

/**
  * _puts - Prints a string
  * @str: The string to print
  *
  * Description: prints a string
  * on success: returns no error
  */

void _puts(char *str)
{
	int 1 = 0;

	while (*(str + i) != '\0')
	{
		putchar(*(str + i));
		i++;
	}
	putchar(10);
}

